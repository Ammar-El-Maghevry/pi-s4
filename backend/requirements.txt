# --- Framework web ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- Base de données ---
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9
pgvector>=0.2.5

# --- Validation / configuration ---
pydantic>=2.6
pydantic-settings>=2.2
email-validator>=2.1

# --- Sécurité / authentification ---
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
# passlib 1.7.4 (dernière version publiée) ne supporte pas bcrypt>=4.1 : son
# détecteur de version lit `bcrypt.__about__`, retiré dans les versions récentes,
# ce qui casse le hachage des mots de passe (AttributeError au premier login).
bcrypt>=4.0,<4.1
python-multipart>=0.0.9

# --- Rapports (export Excel / PDF) ---
openpyxl>=3.1
reportlab>=4.0

# --- Caméras (test de connexion aux flux) ---
opencv-python-headless>=4.9

# --- Tests ---
pytest>=8.0
